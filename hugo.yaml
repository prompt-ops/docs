baseURL: https://pops.sh
title: Prompt-Ops

# Language settings
contentDir: content/en
defaultContentLanguage: en
defaultContentLanguageInSubdir: false

# Useful when translating.
enableMissingTranslationPlaceholders: true

enableRobotsTXT: true

# Will give values to .Lastmod etc.
enableGitInfo: true

languages:
  en:
    languageName: English
    title: Prompt-Ops
    params:
      description: "Prompt-Ops lets you talk to your infrastructure."

markup:
  goldmark:
    parser:
      attribute:
        block: true
    renderer:
      unsafe: true
  highlight:
    # See a complete list of available styles at https://xyproto.github.io/splash/docs/all.html
    style: tango
    # Uncomment if you want your chosen highlight style used for code blocks without a specified language
    # guessSyntax: true

params:
  # Repository configuration (URLs for in-page links to opening issues and suggesting changes)
  github_repo: https://github.com/prompt-ops/pops

  # An optional link to a related project repo. For example, the sibling repository where your product code lives.
  github_project_repo: https://github.com/prompt-ops/pops

  # Uncomment this if your GitHub repo does not have "main" as the default branch,
  # or specify a new value if you want to reference another branch in your GitHub links
  github_branch: main

  ui:
    # Set to true to disable breadcrumb navigation.
    breadcrumb_disable: false
    # Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top navbar
    navbar_logo: true
    # Set to true if you don't want the top navbar to be translucent when over a `block/cover`, like on the homepage.
    navbar_translucent_over_cover_disable: false
    # Enable to show the side bar menu in its compact state.
    sidebar_menu_compact: false
    # Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
    sidebar_search_disable: false

  links:
    # End user relevant links. These will show up on left side of footer and in the community page if you have one.
    user:
      - name: User mailing list
        url: https://pops.sh/email
        icon: fa fa-envelope
        desc: Discussion and help from your fellow users
      # - name: Twitter
      #   url: https://example.org/twitter
      #   icon: fab fa-twitter
      #   desc: Follow us on Twitter to get the latest news!
      # - name: Stack Overflow
      #   url: https://example.org/stack
      #   icon: fab fa-stack-overflow
      #   desc: Practical questions and curated answers
    # Developer relevant links. These will show up on right side of footer and in the community page if you have one.
    developer:
      - name: GitHub
        url: https://github.com/prompt-ops/pops
        icon: fab fa-github
        desc: Development takes place here!
      # - name: Slack
      #   url: https://example.org/slack
      #   icon: fab fa-slack
      #   desc: Chat with other project developers
      # - name: Developer mailing list
      #   url: https://example.org/mail
      #   icon: fa fa-envelope
      #   desc: Discuss development issues around the project

    # Adds a H2 section titled "Feedback" to the bottom of each doc. The responses are sent to Google Analytics as events.
    # This feature depends on [services.googleAnalytics] and will be disabled if "services.googleAnalytics.id" is not set.
    # If you want this feature, but occasionally need to remove the "Feedback" section from a single page,
    # add "hide_feedback: true" to the page's front matter.
    feedback:
      enable: true
      # The responses that the user sees after clicking "yes" (the page was helpful) or "no" (the page was not helpful).
      "yes": >-
        Glad to hear it! Please <a href="https://github.com/prompt-ops/pops/issues/new">tell us how we can improve</a>.
      "no": >-
        Sorry to hear that. Please <a href="https://github.com/prompt-ops/pops/issues/new">tell us how we can improve</a>.

module:
  proxy: direct
  hugoVersion:
    extended: true
    min: 0.73.0
  imports:
    - path: github.com/google/docsy
      disable: false
